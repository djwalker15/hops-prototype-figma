
  # Waste Logging System MVP

  This is a code bundle for Waste Logging System MVP. The original project is available at https://www.figma.com/design/204QKY1j9p6JXPxlaLIMWb/Waste-Logging-System-MVP.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  